version: '3.8'

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: blood_donor_admin
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
