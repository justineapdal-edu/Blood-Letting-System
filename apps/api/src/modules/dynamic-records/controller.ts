export class DynamicRecordsController {
  // GET /api/records/:formId — list all records for a dynamic table
  // POST /api/records/:formId — insert a new record
  // PUT /api/records/:formId/:id — update a record
  // DELETE /api/records/:formId/:id — delete a record
}
