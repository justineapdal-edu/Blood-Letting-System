export class WebhookController {
  // POST /api/webhook — receives Google Apps Script payloads
  // Validates token from Authorization header
  // Dispatches to correct table via payload-parser + dispatcher
}
