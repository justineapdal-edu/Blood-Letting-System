export class FormIntegrationController {
  // POST /api/forms — create a new form connection, generate token
  // GET /api/forms — list all form connections
  // DELETE /api/forms/:id — revoke a form connection
}
